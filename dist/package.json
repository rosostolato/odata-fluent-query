{
  "name": "odata-fluent-query",
  "version": "2.2.1",
  "description": "OData fluent query builder",
  "main": "dist/index.js",
  "repository": {
    "url": "https://github.com/rosostolato/odata-fluent-query"
  },
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:debug": "node --nolazy --inspect-brk ./node_modules/.bin/jest --runInBand --no-cache --watch",
    "build": "tsc && copyfiles package.json README.md dist",
    "publish": "cd ./dist && npm publish",
    "release": "npm run build && npm run publish"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^27.4.0",
    "copyfiles": "^2.4.1",
    "jest": "^27.5.1",
    "ts-jest": "^27.1.3",
    "ts-node": "^10.5.0",
    "typescript": "^4.5.5"
  }
}